# Command: mcp

Runtime entry point for starting or configuring the local Stdio Model Context Protocol (MCP) JSON-RPC 2.0 server.

## Dispatch

1. Load `../workflows/launch-mcp.md`
2. Load `../memory/cli-reference.md`
3. Launch MCP server: `node scripts/mcp_server.js` or `tidy mcp`
