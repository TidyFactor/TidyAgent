/**
 * @file scripts/parallel-orchestrator.js
 * Root proxy to authoritative @tidy/core parallel-orchestrator engine.
 * Maintained for backwards compatibility with root entry points and subagents runner.
 */
module.exports = require('../packages/core/src/parallel-orchestrator');
