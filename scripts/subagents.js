/**
 * @file scripts/subagents.js
 * Root proxy to authoritative @tidy/core subagents engine.
 * Maintained for backwards compatibility with root entry points and skill manifests.
 */
module.exports = require('../packages/core/src/subagents');
