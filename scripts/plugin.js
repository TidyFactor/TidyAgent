/**
 * @file scripts/plugin.js
 * Root proxy to authoritative @tidy/plugin engine.
 * Maintained for backwards compatibility with root entry points and host adapters.
 */
module.exports = require('../packages/plugin/src/index');
