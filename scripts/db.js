/**
 * @file scripts/db.js
 * Root proxy to authoritative @tidy/core database engine.
 * Maintained for backwards compatibility with root entry points and skill manifests.
 */
module.exports = require('../packages/core/src/db');
