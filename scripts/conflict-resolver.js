/**
 * @file scripts/conflict-resolver.js
 * Root proxy to authoritative @tidy/core conflict-resolver engine.
 * Maintained for backwards compatibility with root entry points and subagents dispatcher.
 */
module.exports = require('../packages/core/src/conflict-resolver');
